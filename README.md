# Movie-Flask-SQLAlchemy

# Install following
python3 -m venv <venv_name>

source <venv_name>/bin/activate

pip install flask

pip install dotenv

pip install requests

# create .env and write API_KEY="a0e3f8d3"